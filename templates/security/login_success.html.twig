Добро пожаловать,
{% if app.user.firstname is defined %}{{ app.user.firstname }} {{ app.user.middlename }}
{% else %}{{ app.user.name }}
{% endif %}!<br>

<a href="{{ path('logout') }}"><i class="glyphicon glyphicon-log-out"></i> Выйти</a>

{% if is_granted('ROLE_ADMIN') %}<a href="{{ path('admin-history') }}"><i class="glyphicon glyphicon-log-in"></i> CMS</a>
{% else %}
    {% if is_granted('ROLE_MANAGER') %}<a href="{{ path('admin-table') }}"><i class="glyphicon glyphicon-log-in"></i> CMS</a>
    {% endif %}
{% endif %}



{% if is_granted('ROLE_ORG') %}<a href="{{ path('registration-3') }}"><i class="glyphicon glyphicon-user"></i> Личный кабинет</a>
{% endif %}
import React from 'react';
import {
    Dialog,
    DialogTitle,
    DialogContent,
    DialogActions,
} from '@material-ui/core';
import { Formik } from 'formik';

function ProgramMemberForm({ onClose }) {


    return (
        <Dialog>
            <DialogTitle></DialogTitle>
        </Dialog>
    );
}

export default ProgramMemberForm;